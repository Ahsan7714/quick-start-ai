# The backend API URL for Vercel deployment (pointing to your deployed Express backend)
NEXT_PUBLIC_API_URL=https://your-backend-vercel-url.vercel.app/api/v1

# Optional: Plausible Analytics domain & custom tracking domain (can be left blank)
NEXT_PUBLIC_DOMAIN=
NEXT_PUBLIC_PLAUSIBLE_CUSTOM_DOMAIN=
